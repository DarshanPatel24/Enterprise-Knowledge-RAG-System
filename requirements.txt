# Reference implementation runtime stack (local-first, self-hostable).
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
sqlalchemy>=2.0.0

# Orchestration and agents (behind engine-owned abstractions).
langchain>=0.2.0
langchain-ollama>=0.2.0
langgraph>=0.1.0
# Local HuggingFace dependencies (for Embeddings & Intelligence)
langchain-huggingface>=0.0.3
torch>=2.0.0
transformers>=4.0.0
accelerate>=0.21.0

# Observability (self-hosted, privacy-preserving).
langfuse>=2.0.0
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0

# Infrastructure clients.
qdrant-client>=1.9.0
redis>=5.0.0

# Local model runtime (self-hosted embeddings and chat behind engine seams).
ollama>=0.3.0

# Microsoft SQL Server control plane driver (local, enterprise-ready).
pyodbc>=5.1.0

