/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Local-first: no telemetry, no external image domains, no analytics.
  poweredByHeader: false,
};

export default nextConfig;
