# Developer tooling and quality gates (local-first).
ruff>=0.5.0
mypy>=1.10.0
pytest>=8.2.0
pre-commit>=3.7.0
detect-secrets>=1.5.0

# Local HuggingFace dependencies (for Embeddings & Intelligence).
# NOTE: `torch>=2.0.0` from PyPI installs the CPU-only build. For NVIDIA GPU
# acceleration, install a CUDA build that matches your driver INSTEAD, e.g. (CUDA 13):
#   pip install torch==2.12.1 --index-url https://download.pytorch.org/whl/cu130
# (use cu128/cu126 for older drivers). Then set, in services/ekie/.env:
#   EKIE_EMBEDDING__DEVICE=auto
#   EKIE_EMBEDDING__TORCH_DTYPE=float16   # halves VRAM so large models fit small GPUs
langchain-huggingface>=0.0.3
torch>=2.0.0
transformers>=4.0.0
accelerate>=0.21.0