# Developer tooling and quality gates (local-first).
ruff>=0.5.0
mypy>=1.10.0
pytest>=8.2.0
pre-commit>=3.7.0
detect-secrets>=1.5.0

# Local HuggingFace dependencies (for Embeddings & Intelligence)
langchain-huggingface>=0.0.3
torch>=2.0.0
transformers>=4.0.0
accelerate>=0.21.0