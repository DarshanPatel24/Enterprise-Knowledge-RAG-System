# EKCP runtime dependencies (governed conversational orchestration).
# Config-selected provider clients are imported lazily behind engine seams; the
# set below enables the live chat model, agent runtime, cache, and control
# plane. For the offline default path only fastapi/pydantic are strictly needed.
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- Orchestration (LangChain / LangGraph agent runtime + Ollama seam) ---
langchain-core>=0.2.0
langchain-ollama>=0.2.0
langgraph>=0.2.0

# --- Observability (self-hosted Langfuse LLM tracing) ---
# The LangChain CallbackHandler (langfuse.langchain) requires the langchain
# meta-package in addition to langchain-core. langfuse v3 matches the v3 server.
langfuse>=3.0,<4.0
langchain>=1.0,<2.0

# --- Control-plane and cache backends ---
sqlalchemy>=2.0.0
redis>=5.0.0
httpx>=0.27.0

# --- Local chat model (HuggingFace, GPU-capable) ---
# NumPy is required by torch/transformers at runtime. For NVIDIA GPU acceleration
# install a CUDA build of torch matching your driver INSTEAD of the default CPU
# wheel, e.g. (CUDA 13):
#   pip install torch==2.12.1 --index-url https://download.pytorch.org/whl/cu130
numpy>=1.26
torch>=2.0.0
transformers>=4.0.0
accelerate>=0.21.0
huggingface-hub>=0.23
langchain-huggingface>=0.0.3

# --- Test tooling (needed to run the EKCP test suite: python -m pytest) ---
pytest>=8.2.0
pytest-asyncio>=0.23.0
