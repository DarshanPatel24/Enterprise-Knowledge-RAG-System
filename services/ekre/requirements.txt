# EKRE retrieval platform foundations (EKRE-S0).
# The service is a REST client of the local container stack; all values are
# configuration-driven and installed on demand behind engine-owned seams.

fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- Live retrieval path (Qdrant vector store + Ollama/LangChain seams) ---
langchain-core>=0.2.0
langchain-ollama>=0.2.0
langchain-qdrant>=0.1.0
qdrant-client>=1.9.0

# --- Query embedding + cross-encoder reranker (HuggingFace, GPU-capable) ---
# NumPy is required by torch/transformers/sentence-transformers at runtime.
# For NVIDIA GPU acceleration install a CUDA build of torch matching your driver
# INSTEAD of the default CPU wheel, e.g. (CUDA 13):
#   pip install torch==2.12.1 --index-url https://download.pytorch.org/whl/cu130
# Then set EKRE_EMBEDDING__DEVICE=auto and EKRE_EMBEDDING__TORCH_DTYPE=float16.
numpy>=1.26
torch>=2.0.0
transformers>=4.0.0
accelerate>=0.21.0
sentence-transformers>=3.0.0
huggingface-hub>=0.23
langchain-huggingface>=0.0.3
# bge-base / bge-reranker-base are text-only models and do NOT require
# torchvision. pillow is a transitive sentence-transformers dependency; it is
# pinned here only to guarantee it is present.
pillow>=10.0

# --- Test tooling (needed to run the EKRE test suite: python -m pytest) ---
pytest>=8.2.0
pytest-asyncio>=0.23.0

