"""Contract package version and compatibility metadata."""

from typing import Final

CONTRACTS_VERSION: Final[str] = "1.0.0"
"""Semantic version of the shared contract schemas."""

MIN_SUPPORTED_CONTRACTS_VERSION: Final[str] = "1.0.0"
"""Oldest contract version guaranteed to remain compatible."""
